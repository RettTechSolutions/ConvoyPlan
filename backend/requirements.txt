fastapi==0.136.3
# starlette is pulled in by fastapi; pin to >=1.3.1 to clear PYSEC-2026-161,
# CVE-2024-47874, CVE-2025-54121, CVE-2026-54282 and CVE-2026-54283
# (all <0.47.2/<1.3.1 are vulnerable). fastapi 0.136.3 allows starlette>=0.46.0.
starlette==1.3.1
defusedxml==0.7.1
uvicorn[standard]==0.49.0
sqlalchemy[asyncio]==2.0.50
# asyncpg 0.31.0 is the first release with cp314 wheels (needed for the
# python:3.14-slim base image; 0.29.0 has no 3.14 wheel and fails to build).
asyncpg==0.31.0
geoalchemy2==0.20.0
alembic==1.18.4
pydantic[email]==2.13.4
# PyJWT replaces python-jose (CVE-2024-33663, CVE-2024-33664 — algorithm confusion / sig bypass).
PyJWT==2.13.0
pydantic-settings==2.14.1
cryptography==48.0.1
shapely==2.1.2
python-multipart==0.0.32
bcrypt==5.0.0
httpx==0.28.1
gpxpy==1.6.2
fpdf2==2.8.7
pyotp==2.9.0
aiosmtplib==5.1.1
pytest>=9.0.3
pytest-asyncio>=1.4.0
