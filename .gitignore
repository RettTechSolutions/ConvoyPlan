# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
dist/

# Node
node_modules/
frontend/.svelte-kit/
frontend/build/
frontend/dist/

# Docker volumes / generierte Daten
postgres_data/
graphhopper_cache/
*.osm.pbf
*.osm.pbf.tmp
graphhopper/graph/
graphhopper/graph-cache/

# Environment — never commit secrets
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local

# Deployment secrets (mTLS certs für mobilithek-Feeds etc.) — nie committen
/secrets/
*.pem
*.p12

# License private key and issuer tool — admin-only, never commit
scripts/convoyplan_license.key
scripts/issue_license.py

# Server-spezifische Konfiguration (nie öffentlich einchecken)
docker-compose.override.yml

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temp
*.tmp
*.bak

# Git worktrees
.worktrees/

# Claude Code — lokale IDE-Konfiguration (MCP-Server, Pfade, etc.)
.claude/

# Claude Code / Superpowers internal tooling
.superpowers/
.remember/
# docs/ ist tracked — enthält Specs und Design-Dokumente
